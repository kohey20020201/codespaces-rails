<h1>タスクを編集</h1>

<%= form_with(model: @task, url: "/tasks/#{@task.id}") do |f| %>
    <label for="content">内容：</label>
    <%= f.text_field :content, id: "content" %>
    <br>
    <label for="due_date">期限：</label>
    <%= f.date_field :due_date, id: "due_date" %>
    <br>
    <%= f.submit "更新" %>
<% end %>